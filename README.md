# replacement-trainval_merged.json-Pascal-Context-59-mmsegmentation
Script to replace pipeline (with the Detail API) that needed trainval_merged.json which cannot be downloaded anymore (deadlink)

Uses a lookup table to create train and val .png images in the format expected by mmsegmentation.

The val dataset is also the test set on which contemporary numbers are published.
-------

Converter script is placed in ../data/VOCdevkit/VOC2010 and run from ..

Generative AI was used to create this script.
